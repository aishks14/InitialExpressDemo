html
html
    head
        title Form Tester
    body
        form(action = "/", method = "POST")
            div
                h1 FORM APPLICATION  #{title}
            div
                label(for = "say") Say:
                input(name = "say" value = "Hi")
            br
            div
                label(for = "to") To:
                input(name = "to" value = "Express forms")
            br
            button(type = "submit") Send My Greetings
